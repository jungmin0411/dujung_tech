import React from "react";
import ReactDOM from "react-dom/client";
import InspectionDashboard from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <InspectionDashboard />
  </React.StrictMode>
);
